services:
  - type: web
    name: defi-etl-platform
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0